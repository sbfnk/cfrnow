% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_cfr.R
\name{default_delay}
\alias{default_delay}
\title{Default onset-to-death delay specification}
\usage{
default_delay()
}
\value{
A \code{dist_spec} for the onset-to-death delay.
}
\description{
A \code{\link[dist.spec:LogNormal]{dist.spec::LogNormal()}} with \code{Normal()} priors on the native parameters,
reproducing the BDBV/Ebolavirus onset-to-death prior of the Julia reference
model (the Isiro 2012 line-list reanalysis): \code{meanlog ~ Normal(2.41, 0.2)},
\code{sdlog ~ Normal(0.51, 0.15)} (delay mean ~= 12.75 d, sd ~= 7 d).
}
\details{
Supply your own \code{\link[dist.spec:LogNormal]{dist.spec::LogNormal()}} or \code{\link[dist.spec:Gamma]{dist.spec::Gamma()}} to
\code{\link[=fit_cfr]{fit_cfr()}} to change the family or priors. Give each native parameter as a
\code{Normal()} to co-estimate it, or as a fixed number / \code{\link[dist.spec:Fixed]{dist.spec::Fixed()}} to
hold it fixed (fixing the whole delay gives the Ghani/Nishiura estimator).
}
\examples{
default_delay()
}
