% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_cfr.R
\name{cfr_prior}
\alias{cfr_prior}
\title{CFR prior, specified in CFR units}
\usage{
cfr_prior(mean = 0.5, sd = 0.2)
}
\arguments{
\item{mean}{Prior mean of the CFR, on the [0, 1] scale.}

\item{sd}{Prior sd of the CFR, on the [0, 1] scale.}
}
\value{
A \code{brmsprior} on the \code{cfr} intercept.
}
\description{
The CFR enters the model on the logit scale (it has covariates through the
formula), so its prior is a Normal on \code{logit(cfr)} rather than a \code{Beta}. This
helper lets you think in CFR units instead: it moment-matches a logit-Normal
whose induced mean and sd on the [0, 1] CFR scale are approximately \code{mean}
and \code{sd}, and returns the corresponding \code{brms} prior on the \code{cfr} intercept.
}
\details{
Rough guides: \code{cfr_prior(0.5, 0.29)} is near-flat (the default);
\code{cfr_prior(0.1, 0.1)} favours a low CFR; \code{cfr_prior(0.33, 0.12)} suits a
high-fatality pathogen. The CFR is weakly identified early in an outbreak, so
this prior can dominate — choose it deliberately.
}
\seealso{
Other fit:
\code{\link[=cfr_default_prior]{cfr_default_prior()}},
\code{\link[=fit_cfr]{fit_cfr()}},
\code{\link[=fix_delay]{fix_delay()}},
\code{\link[=summary.cfrnow_fit]{summary.cfrnow_fit()}}
}
\concept{fit}
