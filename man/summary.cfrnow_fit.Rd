% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise_cfr.R
\name{summary.cfrnow_fit}
\alias{summary.cfrnow_fit}
\alias{print.cfrnow_fit}
\title{Summarise a mixture-cure CFR fit}
\usage{
\method{summary}{cfrnow_fit}(
  object,
  probs = c(0.025, 0.5, 0.975),
  info_tol = 0.9,
  ascertainment_ratio = 1,
  ...
)

\method{print}{cfrnow_fit}(x, ...)
}
\arguments{
\item{object}{A \code{cfrnow_fit} from \code{\link[=fit_cfr]{fit_cfr()}}.}

\item{probs}{Quantiles to report.}

\item{info_tol}{Low-information threshold: flag when the CFR posterior sd is
more than this fraction of the prior sd. Defaults to 0.9.}

\item{ascertainment_ratio}{Ratio \code{r} of the ascertainment probability of
fatal to non-fatal cases (see Details). A single positive number; defaults
to 1 (no correction).}

\item{...}{Unused.}

\item{x}{A \code{cfrnow_fit}.}
}
\value{
A data frame with one row per quantity: \code{cfr} (or one \verb{cfr[<group>]}
row per group for a \code{cfr ~ group} fit), \code{delay_mean} and \code{delay_sd},
carrying \code{naive_cfr}, \code{n_cases}, \code{n_deaths}, \code{cfr_prior_sd},
\code{cfr_low_information} and \code{ascertainment_ratio} attributes.
}
\description{
Reports the corrected CFR and the onset-to-death delay (mean and sd, in days)
as posterior quantiles with convergence diagnostics (\code{rhat}, \code{ess_bulk}). The
naive \code{deaths / cases} ratio is returned as an attribute; in real time it
underestimates the corrected CFR because not every fatal case has died by the
cut-off.
}
\details{
When few deaths have resolved (a young outbreak) the CFR is only weakly
identified and its posterior stays close to the prior. This is reported via
the \code{cfr_low_information} attribute: \code{TRUE} when the CFR posterior sd exceeds
\code{info_tol} times the prior sd.

For a \code{cfr ~ group} fit the CFR varies by group, so one \verb{cfr[<group>]} row is
reported per group (grouping predictors must be factors or characters), and
the \code{cfr_low_information} flag is \code{NA} (only defined for a single CFR).

The CFR the model fits is the fatality risk among \emph{ascertained} cases. When
ascertainment is outcome-dependent -- fatal and non-fatal cases entering the
line list at different rates -- this differs from the population CFR.
\code{ascertainment_ratio} (\code{r}) is the ratio of the ascertainment probability of
fatal to non-fatal cases; the reported CFR is shifted on the logit scale by
\code{-log(r)}, so \code{r} > 1 (fatal cases over-ascertained) lowers it and \code{r} < 1
(e.g. deaths not linked back to cases) raises it. It is supplied, not fitted,
and defaults to 1 (no correction); because the correction is a post-hoc logit
shift, sweep a range of \code{r} to show its leverage rather than trusting a
single value.
}
\seealso{
Other fit: 
\code{\link{fit_cfr}()},
\code{\link{pp_check_cfr}()}
}
\concept{fit}
