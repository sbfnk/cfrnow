% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_linelist.R
\name{simulate_linelist}
\alias{simulate_linelist}
\title{Simulate a line list for testing and examples}
\usage{
simulate_linelist(
  n = 200,
  cfr = 0.5,
  delay,
  recovery = NULL,
  onset_start = as.Date("2026-01-01"),
  onset_days = 60
)
}
\arguments{
\item{n}{Number of cases.}

\item{cfr}{True case fatality ratio.}

\item{delay}{Onset-to-death delay: a distspec distribution
(\code{\link[distspec:LogNormal]{distspec::LogNormal()}} or \code{\link[distspec:Gamma]{distspec::Gamma()}}) with fixed parameters.}

\item{recovery}{Optional onset-to-recovery delay (same form as \code{delay}); when
given, non-fatal cases get a \code{recovery_date}.}

\item{onset_start}{First possible onset date.}

\item{onset_days}{Width of the onset window (days); onsets are uniform
over it.}
}
\value{
A data frame with \code{onset_date}, \code{death_date} and, if \code{recovery} is
given, \code{recovery_date}.
}
\description{
Draws onset dates over a window, marks each case fatal with probability
\code{cfr}, and gives fatal cases an onset-to-death delay drawn from \code{delay}. Pass
a \code{recovery} delay to also simulate onset-to-recovery times for the non-fatal
cases and add a \code{recovery_date} column. Delays are distspec distributions
with fixed parameters, matching what \code{\link[=fit_cfr]{fit_cfr()}} takes; you can specify them
by mean and sd (e.g. \code{distspec::LogNormal(mean = 12.75, sd = 7)}). Returns a
line list with \code{onset_date}, \code{death_date} (\code{NA} for non-fatal cases) and,
when \code{recovery} is given, \code{recovery_date} (\code{NA} for fatal cases). The full,
untruncated outcomes are simulated; pass the result to \code{\link[=prepare_cfr_data]{prepare_cfr_data()}}
with an \code{obs_time} to induce the real-time truncation.
}
\details{
Data are generated to match the model's daily interval-censoring: each case's
true onset falls uniformly within its recorded day, and the event day is the
floor of the continuous onset-plus-delay time. So the recorded day-level
delays are exactly a doubly-interval-censored draw, which makes the simulator
suitable for checking calibration, not only rough recovery.
}
\examples{
simulate_linelist(n = 5, cfr = 0.6,
                  delay = distspec::LogNormal(mean = 12.75, sd = 7))
}
